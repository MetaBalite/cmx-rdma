"""SGLang cache backend implementation (Phase 2 stub).

This module will implement SGLang's cache backend interface, mapping
RadixAttention's token-level granularity to block-level with padding.

For Phase 1, SGLang integration goes through LMCache's abstraction layer
(see cmx-lmcache). This direct connector is for SGLang deployments
that don't use LMCache.
"""

# Phase 2: Implement SGLang's cache backend interface directly.
# For now, use cmx-lmcache for SGLang integration via LMCache.
